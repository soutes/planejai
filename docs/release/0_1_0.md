# planejAÍ v0.1.0 — Release Report

**Data:** 2026-05-21  
**Versão:** 0.1.0 (MVP)  
**Status:** LANÇADO

---

## Resumo executivo

planejAÍ v0.1.0 é a reescrita completa em TypeScript do sistema de gestão financeira pessoal anteriormente implementado em Python/Streamlit. O MVP entrega as 10 user stories do escopo, cobrindo o ciclo completo de controle financeiro pessoal: despesas, rendimentos, investimentos, cartões de crédito com análise de fatura por IA, dashboard consolidado, relatório executivo com IA, e gestão de pessoas/splits.

A arquitetura foi validada pelo architect-agent e 100% das US passaram em QA funcional. O sistema roda localmente com dois terminais — sem dependência de cloud, sem autenticação, sem build complexo.

---

## Funcionalidades entregues

### US-01 — Registrar Despesa (`/despesas`)
- CRUD completo com validação Zod
- Suporte a tipos: manual, fixa, parcela, cartão, sintéticos (cartao_ciclo, split_auto)
- Parcelamento com `origemId` vinculando série de parcelas
- Delete em série (`?serie=true`) remove despesa origem e todas filhas
- Splits entre pessoas (proporção configurável por despesa)
- Filtro automático de tipos sintéticos na listagem (split_auto, cartao_ciclo excluídos da view manual)

### US-02 — Registrar Rendimento (`/rendimentos`)
- CRUD com categorias: Salário, Aluguel, Freelas, Dividendos, Outros
- Recorrência: `recorrente=true` + `totalRepeticoes=N` cria N registros com mesRef incrementado mensalmente, todos com `origemId` compartilhado
- Delete em série (`?serie=true`)
- Gráfico pizza por categoria no frontend

### US-03 — Registrar Investimento (`/investimentos`)
- Snapshot mensal por categoria e instituição
- Upsert idempotente: `POST /api/investimentos` atualiza se (mesRef + categoria + instituição) já existe
- Categorias: Reserva de Emergência, Renda Fixa, Tesouro Direto, Ações, FIIs, Previdência Privada, Fundos, Cripto, Internacional
- Gráfico donut e evolução histórica no frontend

### US-04 — Analisar Fatura de Cartão via IA (`/cartao`)
- Upload PDF/imagem → `POST /api/intelligence/analyze-pdf` → Claude extrai JSON estruturado
- Deduplicação por SHA-256 do arquivo (reimport detectado com erro 409)
- Edição de categoria por transação: `PUT /api/faturas/:id/transacoes/:transacaoId`
- Retorna `FaturaAnalisada { fatura, transacoes[], resumo_categorias[], comentario_executivo }`
- Fallback mock no frontend quando API offline

### US-05 — Acompanhar Ciclo Atual do Cartão (`/cartao`)
- Ciclo calculado via `diaFechamento` configurável (não é mês calendário)
- Snapshot do ciclo: `GET/POST/DELETE /api/snapshots`
- Delta percentual vs ciclo anterior
- Indicador visual de utilização de limite com alerta em >80%

### US-06 — Ver Dashboard Financeiro (`/dashboard`)
- Endpoint único `GET /api/dashboard?mesRef=YYYY-MM` sem N+1
- Agrega: total despesas reais (excluindo split_auto e cartao_ciclo), total rendimentos, total investido, saldo do mês
- Inclui: despesas por categoria, orçamentos por categoria com % atingido, divisões pendentes por pessoa
- Filtro de sintéticos correto — sem dupla contagem

### US-07 — Consultar Relatório com IA (`/relatorio`)
- `POST /api/intelligence/report` com `{ mesRef: "YYYY-MM" }`
- Contexto enviado à IA: total despesas/rendimentos/investimentos + breakdown por categoria (apenas agregações — nenhuma transação individual)
- Resposta estruturada: `RelatorioIA { titulo, resumo, destaques[], alertas[], recomendacoes[], comentario_final }`
- Frontend converte para Markdown com seções renderizadas
- **Requer ANTHROPIC_API_KEY** — testado estruturalmente, e2e depende de key válida

### US-08 — Gerenciar Cartões (`/gestao`)
- CRUD completo com validação
- Soft delete: `DELETE /api/cartoes/:id` marca `ativo=false` (nunca remove do banco)
- Cartão sentinela `id=1` protegido: retorna 400 se tentativa de exclusão
- Campos: nome, bandeira, limite, diaFechamento, cor, final (4 dígitos), pessoaId, abaId

### US-09 — Gerenciar Pessoas e Splits (`/gestao`)
- CRUD pessoas
- `GET /api/divisao` — lista divisões pendentes/quitadas com filtro por pessoaId e quitado
- `POST /api/divisao` — cria entrada manual de divisão
- `PUT /api/divisao/:id` — quita divisão (marca `quitado=true`, nunca deleta)
- DivisaoEntry nunca é excluída — imutabilidade do histórico financeiro

### US-10 — Configurar Categorias, Abas e Metas (`/gestao`)
- CRUD categorias com flag `permanente` (categorias permanentes não são deletáveis)
- CRUD abas com suporte a orçamentos por aba
- Orçamentos: `PUT /api/orcamentos` (upsert por abaId + mesRef + categoria)
- `DELETE /api/orcamentos/:id` retorna 404 para id inexistente (não 500)

---

## Stack técnica

| Camada | Tecnologia |
|--------|------------|
| Backend runtime | Node.js + Fastify 5 |
| Linguagem | TypeScript (strict) |
| ORM | Prisma 5 + SQLite |
| Validação | Zod + fastify-type-provider-zod |
| IA | Anthropic SDK (`claude-sonnet-4-6`) com prompt caching |
| Frontend | Next.js 15 App Router |
| UI | Server Components + 'use client' seletivo |
| Gráficos | Recharts |
| Ícones | lucide-react |
| Fontes | Bricolage Grotesque + Plus Jakarta Sans + JetBrains Mono |

**Arquitetura backend:** DDD enxuto com 2 bounded contexts (`finances`, `intelligence`), 4 camadas explícitas (domain → application → infra → http), DI manual sem container, sem decorators.

---

## Decisões de negócio e técnicas

| ID | Decisão |
|----|---------|
| DEC-001 | SQLite — app local offline-first, sem Postgres |
| DEC-002 | Anthropic TypeScript SDK — sem CLI subprocess Python |
| DEC-003 | Sem deploy cloud — execução local com dois terminais |
| DEC-004 | Sem autenticação — single-user no MVP |
| DEC-005 | Valores em Float reais — não centavos (consistência com legado) |
| DEC-PO-003 | `PUT /api/faturas/:id/transacoes/:transacaoId` obrigatório para edição de categoria pós-análise IA |
| DEC-PO-004 | `GET/POST/PUT /api/divisao` obrigatórios; DivisaoEntry nunca deletada — apenas `quitado=true` |
| DEC-PO-005 | `GET/POST/PUT /api/abas` obrigatórios para gestão de abas no MVP |

---

## Bugs encontrados e corrigidos durante o ciclo

| # | Módulo | Bug | Fix aplicado |
|---|--------|-----|--------------|
| 1 | finances.module.ts | Prefixo `/api` ausente em todas as rotas | Wrapper com `{ prefix: '/api' }` |
| 2 | despesas.routes.ts | Query param `deleteAll` em vez de `serie` | Renomeado para `serie` |
| 3 | rendimentos.routes.ts | Mesmo que #2 | Mesmo fix |
| 4 | prisma-cartao.repository.ts | Hard delete de cartão (FK violation) | Soft delete `ativo=false` |
| 5 | delete-cartao.use-case.ts | Cartão sentinela id=1 podia ser deletado | Guard `if (id === 1) throw 400` |
| 6 | get-dashboard.use-case.ts | Dupla contagem: sintéticos incluídos no total | Filtro `tipo NOT IN (split_auto, cartao_ciclo)` |
| 7 | list-despesas.use-case.ts | Tipos sintéticos apareciam na listagem manual | Filtro `TIPOS_SINTETICOS` |
| 8 | create-rendimento.use-case.ts | Recorrência criava apenas 1 registro | Loop com `addMonths()` cria N registros |
| 9 | AnalyzePdfUseCase.ts | Domain importava de infra (PROMPTS) | Movido para `domain/prompts/index.ts` |
| 10 | GenerateReportUseCase.ts | Mesmo que #9 + dupla contagem sintéticos | Mesmo fix + filtro despesasReais |
| 11 | investimentos.routes.ts | `app.put` em vez de `app.post` → POST 404 | `app.put` → `app.post` |
| 12 | finances.module.ts | `createDivisao` undefined em runtime (escopo Fastify) | Use cases extraídas para variáveis antes de `api.register` |
| 13 | prisma-despesa.repository.ts | `origemId` ausente no `data` do `create()` | Campo adicionado |
| 14 | despesas.routes.ts | `origemId` ausente no Zod schema → strip silencioso | Campo adicionado ao `CreateDespesaBody` |
| 15 | delete-orcamento.use-case.ts | Prisma P2025 → 500 em vez de 404 | try/catch P2025 → `HttpError.notFound` |

---

## Riscos conhecidos

| Risco | Impacto | Mitigação |
|-------|---------|-----------|
| US-07 (Relatório IA): ANTHROPIC_API_KEY não configurada | Endpoint retorna erro de autenticação; frontend cai no mock | Configurar `ANTHROPIC_API_KEY` no `.env` de `apps/api` |
| US-04 (Analyze PDF): mesma dependência de API key | analyze-pdf falha sem key; frontend usa mock da fatura | Mesmo que acima |
| P2025 Prisma na camada application | Código de infra (Prisma) exposto em `delete-orcamento.use-case.ts` | Refatorar repo para absorver P2025 em versão futura |
| SQLite sem migrations em produção | Mudanças de schema requerem `prisma migrate dev` manual | Documentado no README; Tauri packaging resolverá no futuro |

---

## Instruções de execução

### Pré-requisitos
- Node.js 20+
- npm 10+

### 1. Banco de dados

```bash
cd apps/api
npm install
npx prisma migrate dev --name init
npx prisma db seed    # opcional — dados de exemplo
```

### 2. Variáveis de ambiente

```bash
# apps/api/.env
DATABASE_URL="file:./dev.db"
ANTHROPIC_API_KEY="sk-ant-..."   # necessário para US-04 e US-07
CORS_ORIGIN="http://localhost:3000"
```

### 3. Executar (dois terminais)

```bash
# Terminal 1 — API (porta 3001)
cd apps/api
npm run dev

# Terminal 2 — Frontend (porta 3000)
cd apps/web
npm install
npm run dev
```

Acesse: `http://localhost:3000`

---

## Próximos passos (pós-MVP)

1. **Configurar ANTHROPIC_API_KEY** — habilitar US-04 e US-07 em produção local
2. **Tauri shell** — empacotar como app desktop nativo (Windows/macOS/Linux)
3. **Testes automatizados** — suite de integração para endpoints críticos (dashboard, despesas, splits)
4. **Seed de dados** — popular banco com categorias e abas padrão no primeiro uso
5. **Refatorar P2025** — mover captura de erro Prisma para camada de repositório
6. **Mobile responsivo** — OUT OF SCOPE v0.1.0 (DEC-PO-002)
7. **Autenticação** — OUT OF SCOPE v0.1.0 (DEC-001)
8. **Exportação CSV/PDF** — OUT OF SCOPE v0.1.0 (DEC-PO-002)

---

## Estatísticas do ciclo

| Métrica | Valor |
|---------|-------|
| User Stories entregues | 10/10 |
| Módulos backend aprovados | 16/16 |
| Módulos frontend aprovados | 12/12 |
| Bugs encontrados em QA | 15 |
| Bugs corrigidos | 15 |
| Bugs abertos | 0 |
| Ciclos de review architect | 5 |
| Data de início | 2026-05-20 |
| Data de lançamento | 2026-05-21 |
