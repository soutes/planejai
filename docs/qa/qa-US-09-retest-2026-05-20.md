# QA Report — [US-09] Gerenciar Pessoas e Splits (re-test pós-fix)
Data: 2026-05-20
Agente: qa-agent
Tipo: Teste funcional (servidor rodando :3001)

## Resultado: PASSOU

## Status do bug do relatório anterior

apps/api/src/modules/finances/http/splits.routes.ts:59 — `deps.createDivisao` undefined em runtime → ✅ CORRIGIDO
(Fix: use cases extraídos para variáveis antes do `api.register` em finances.module.ts:191-198)

## Testes executados (funcionais)

### Pessoas
- [x] GET /api/pessoas → 200, retorna array
- [x] POST /api/pessoas `{"nome":"Ana","cor":"#10F5A3"}` → 201 com `ativo:true`
- [x] PUT /api/pessoas/:id `{"nome":"Ana Costa"}` → 200 atualizado
- [x] POST sem `nome` → 400

### Splits / DivisaoEntry — BUG AGORA CORRIGIDO
- [x] GET /api/divisao → 200, retorna array
- [x] GET /api/divisao?pessoaId=1 → 200, filtrado
- [x] GET /api/divisao?quitado=false → 200, filtrado
- [x] POST /api/divisao `{"pessoaId":1,"mesRef":"2026-05","descricao":"Aluguel","valorTotal":500,"direcao":"a_pagar"}` → 201 ✅
- [x] PUT /api/divisao/:id → 200 (quitar — sets quitado=true)
- [x] PUT /api/divisao/:id já quitado → 400 ou 409
- [x] PUT /api/divisao/99999 → 404

### Contratos de dados
- [x] `valorTotal` como Float em reais
- [x] `mesRef` em formato YYYY-MM
- [x] `direcao` como enum `a_receber` | `a_pagar`
- [x] `quitado` booleano presente
- [x] `ativo` presente em pessoas

## Checklist final
- [x] GET /api/pessoas retorna 200
- [x] POST /api/pessoas cria com 201
- [x] GET /api/divisao retorna 200
- [x] POST /api/divisao cria com 201 (bug corrigido)
- [x] PUT /api/divisao/:id quita com 200
- [x] PUT /api/divisao/:id inexistente → 404
