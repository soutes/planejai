# QA Report — [US-08] Gerenciar Cartões (re-test pós-fix)
Data: 2026-05-20
Agente: qa-agent
Tipo: Teste funcional (servidor rodando :3001)

## Resultado: PASSOU

## Status dos bugs do relatório anterior

apps/api/src/modules/finances/infra/prisma-cartao.repository.ts — hard delete em vez de soft delete → ✅ CORRIGIDO
apps/api/src/modules/finances/application/use-cases/delete-cartao.use-case.ts — sentinela id=1 não protegido → ✅ CORRIGIDO

## Testes executados (funcionais)

### Happy path
- [x] GET /api/cartoes → 200, retorna array
- [x] POST /api/cartoes `{"nome":"Nubank","limite":5000}` → 201
- [x] PUT /api/cartoes/:id `{"nome":"Nubank Atualizado"}` → 200 com dados atualizados
- [x] DELETE /api/cartoes/:id → 204

### Soft delete (bug crítico anterior — corrigido)
- [x] DELETE /api/cartoes/:id → cartão permanece no banco com `ativo=false`
- [x] GET /api/cartoes → cartão desativado não aparece na lista (filtrado por `ativo=true`)

### Sentinela id=1 (bug crítico anterior — corrigido)
- [x] DELETE /api/cartoes/1 → 400 "Cartão sentinela não pode ser removido"
- [x] Cartão id=1 continua existindo e acessível após tentativa de delete

### Validação de input
- [x] POST sem `nome` → 400
- [x] DELETE /api/cartoes/99999 → 404

### Contratos de dados
- [x] `ativo` presente e booleano nas respostas
- [x] `limite` como Float em reais (nullable)
- [x] Response inclui `id`, `nome`, `limite`, `cor`, `ativo`, `diaFechamento`

## Checklist final
- [x] GET lista retorna 200
- [x] POST cria recurso com 201
- [x] PUT atualiza recurso com 200
- [x] DELETE desativa (ativo=false) em vez de excluir fisicamente
- [x] Sentinela id=1 protegido de deleção (400)
- [x] DELETE recurso inexistente retorna 404
