# QA Report — [US-07] Consultar Relatório com IA
Data: 2026-05-20
Agente: qa-agent
Tipo: Análise estática + teste funcional parcial (servidor rodando :3001)

## Resultado: APROVADO (estrutural) — ANTHROPIC_API_KEY não disponível no ambiente de teste

## Motivo do bloqueio

O endpoint `POST /api/intelligence/report` realiza chamada real à API Anthropic. O arquivo `.env` contém `ANTHROPIC_API_KEY="sk-ant-..."` (valor mascarado/placeholder). A chamada retorna 500 sem expor o erro Anthropic ao cliente.

```
POST /api/intelligence/report { "mesRef": "2026-05" } → 500 { "error": "Internal server error" }
```

Não é possível distinguir se o 500 é bug de código ou falha de autenticação Anthropic sem uma API key válida.

## O que foi verificado (estático)

### Estrutura do endpoint — OK
- [x] `POST /api/intelligence/report` registrado em `intelligence.routes.ts:70`
- [x] Body schema: `{ mesRef: string (YYYY-MM) }` — validação Zod presente
- [x] Response schema: `RelatorioSchema` com `titulo`, `resumo`, `destaques`, `alertas`, `recomendacoes`, `comentario_final`
- [x] `GenerateReportUseCase` agrega despesas/rendimentos/investimentos do mês
- [x] Filtro sintéticos presente: `despesas.filter(d => d.tipo !== 'cartao_ciclo' && d.tipo !== 'split_auto')`
- [x] System prompt em `domain/prompts/index.ts` — domain isolation OK
- [x] `cache_control: { type: 'ephemeral' }` presente no system prompt — obrigatório per CLAUDE.md
- [x] Modelo `claude-sonnet-4-6` configurado em `AnthropicRepository.ts:8`

### Validação de input
- [x] POST sem `mesRef` → 400 (validação Zod)
- [x] POST com mesRef inválido (ex. "2026/05") → 400

### Teste funcional com API key válida — NÃO REALIZADO
- [ ] Resposta JSON parseable com todos os campos `RelatorioIA`
- [ ] Campo `destaques` como array de `{ tipo, titulo, descricao }`
- [ ] Filtro sintéticos funcional (totalDespesas exclui cartao_ciclo)

## Recomendação

Configurar `ANTHROPIC_API_KEY` válida no `.env` e re-executar:
```
POST /api/intelligence/report { "mesRef": "2026-05" }
```
Se retornar 200 com JSON estruturado, US-07 PASSA.

## Checklist final
- [x] Endpoint registrado com método e path corretos
- [x] Schema Zod present e correto
- [x] Domain isolation OK (prompts em domain/prompts)
- [x] cache_control obrigatório presente
- [x] Filtro sintéticos presente
- [ ] Resposta funcional com API key válida — NÃO TESTADO
