# QA Report — [US-05] Acompanhar Ciclo Atual do Cartão
Data: 2026-05-20
Agente: qa-agent
Tipo: Teste funcional (servidor rodando :3001)

## Resultado: PASSOU

## Testes executados (funcionais)

### Happy path — snapshots
- [x] GET /api/snapshots → 200, retorna array
- [x] GET /api/snapshots?cartaoId=1 → 200, filtra por cartão
- [x] POST /api/snapshots `{"cartaoId":1,"mesRef":"2026-05","gastoTotal":1200,"limite":5000}` → 201
- [x] DELETE /api/snapshots/:id → 204
- [x] DELETE /api/snapshots/99999 → 404

### Validação de input
- [x] POST sem `cartaoId` → 400
- [x] POST com `mesRef` inválido → 400
- [x] POST sem `gastoTotal` → 400

### Contratos de dados
- [x] `mesRef` em formato YYYY-MM — validação regex presente
- [x] `gastoTotal` e `limite` como Float em reais
- [x] Response inclui todos campos esperados: `id`, `cartaoId`, `mesRef`, `gastoTotal`, `limite`, `deltaVsAnterior`

### Dashboard delta (US-05 requer delta vs snapshot anterior)
- [x] `deltaVsAnterior` presente no response de snapshot (nullable quando não há anterior)

## Checklist final
- [x] GET lista retorna 200
- [x] POST cria snapshot com 201
- [x] DELETE remove com 204
- [x] DELETE recurso inexistente retorna 404
- [x] Validação de input funciona (400)
- [x] mesRef YYYY-MM validado
- [x] Valores monetários como Float
