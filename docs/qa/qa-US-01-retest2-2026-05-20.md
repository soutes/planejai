# QA Report — [US-01] Registrar Despesa (re-test 2 pós-fix completo)
Data: 2026-05-20
Agente: qa-agent
Tipo: Teste funcional (servidor rodando :3001)

## Resultado: PASSOU

## Status dos bugs anteriores

- `prisma-despesa.repository.ts:44` — `origemId: input.origemId ?? null` → ✅ CORRIGIDO
- `despesas.routes.ts:53` — `origemId: z.number().int().nullable().optional()` adicionado ao `CreateDespesaBody` → ✅ CORRIGIDO (fix incompleto resolvido)

## Testes executados (funcionais)

### Happy path
- [x] GET /api/despesas → 200, retorna array excluindo cartao_ciclo e split_auto
- [x] POST /api/despesas `{"abaId":1,"mesRef":"2026-05","descricao":"Mercado","categoria":"Alimentacao","valor":250}` → 201
- [x] PUT /api/despesas/:id → 200 atualizado
- [x] DELETE /api/despesas/:id → 204
- [x] DELETE /api/despesas/:id?serie=true → 204

### origemId persistido (bug crítico resolvido)
```
POST /api/despesas {"abaId":1,"mesRef":"2026-05","descricao":"Parcela 1/3","categoria":"Casa","valor":100,"origemId":10}
→ 201 {"id":13,...,"origemId":10,...}  ← origemId corretamente persistido ✅
```

### Filtro sintéticos (bug anterior resolvido em rodada anterior)
- [x] GET /api/despesas não retorna tipo=cartao_ciclo
- [x] GET /api/despesas não retorna tipo=split_auto

### Contratos de dados
- [x] `mesRef` em formato YYYY-MM validado
- [x] `valor` como Float em reais
- [x] `origemId` presente no response e persistido corretamente

## Checklist final
- [x] GET lista retorna 200 (com filtro sintéticos)
- [x] POST cria com 201
- [x] POST com origemId persiste origemId corretamente
- [x] PUT atualiza com 200
- [x] DELETE remove com 204
- [x] DELETE ?serie=true remove série
