# QA Report — [US-09] Gerenciar Pessoas e Splits
Data: 2026-05-20
Agente: qa-agent
Tipo: Teste funcional (servidor rodando :3001)

## Resultado: FALHOU

## Bug encontrado

apps/api/src/modules/finances/http/splits.routes.ts:59 ❌ CRÍTICO: `POST /api/divisao` retorna 500 com `TypeError: Cannot read properties of undefined (reading 'execute')`. O handler acessa `deps.createDivisao.execute(req.body)` mas `deps.createDivisao` é `undefined` em runtime.

Reprodução:
```
POST /api/divisao
{ "pessoaId": 1, "mesRef": "2026-05", "descricao": "Aluguel", "valorTotal": 500 }
→ 500: { "error": "Internal server error" }
Log: TypeError: Cannot read properties of undefined (reading 'execute') at splits.routes.ts:59:46
```

Nota: O código em `finances.module.ts` registra corretamente `createDivisao: new CreateDivisaoUseCase(divisaoRepo)`. A causa exata do `undefined` em runtime não foi isolada — possível conflito de escopo de Fastify plugin, circular dependency ou falha silenciosa na instanciação.

## Testes executados (funcionais)

### Pessoas — PASSOU
- [x] GET /api/pessoas → 200, retorna array
- [x] POST /api/pessoas `{"nome":"Maria","cor":"#B07AFF"}` → 201 `{"id":2,"nome":"Maria","cor":"#B07AFF","ativo":true}`
- [x] PUT /api/pessoas/2 `{"nome":"Maria Silva"}` → 200 com nome atualizado
- [x] POST sem `nome` → 400 com `{"error":"Validation error","details":"body/nome Required"}`
- [x] `ativo` presente e booleano nas respostas

### Splits / DivisaoEntry — FALHOU
- [x] GET /api/divisao → 200, retorna array (vazio ou com dados)
- [x] GET /api/divisao?pessoaId=1 → 200, filtra por pessoa
- [x] GET /api/divisao?quitado=false → 200, filtra por status
- [ ] POST /api/divisao → 500 (CRÍTICO — deps.createDivisao undefined)
- [x] PUT /api/divisao/:id → 200 (quitar divisão funciona quando ID existe)
- [x] PUT /api/divisao/99999 → 404

### Contratos de dados — Pessoas
- [x] `ativo` presente e booleano
- [x] Sem `mesRef` (não aplicável a pessoas)

### Contratos de dados — DivisaoEntry
- [x] GET retorna schema correto: `id`, `pessoaId`, `mesRef`, `descricao`, `valorTotal`, `direcao`, `parcelado`, `quitado`
- [x] `valorTotal` como Float em reais
- [x] `mesRef` em formato YYYY-MM
- [x] `direcao` como enum `a_receber` | `a_pagar`

## Checklist final
- [x] GET /api/pessoas retorna 200
- [x] POST /api/pessoas cria com 201
- [x] PUT /api/pessoas/:id atualiza com 200
- [x] GET /api/divisao retorna 200
- [ ] POST /api/divisao cria com 201 — FALHOU (500)
- [x] PUT /api/divisao/:id quita com 200
- [x] PUT /api/divisao/:id inexistente retorna 404
