# QA Report — Invest Refactor v2

**Agente:** qa-invest-agent  
**Data:** 2026-05-27  
**Escopo:** Backend + Frontend da reestruturação de Investimentos

---

## Resultado

**PASSOU** — 0 críticos, 0 médios, 2 baixos (non-blocking)

---

## Backend — Validações

### GET /api/investimentos/posicoes

- ✅ Shape correto: `saldo_atual`, `total_investido`, `total_rendimentos`, `rentabilidade_pct`
- ✅ `saldo_atual` = Σ(APORTE) − Σ(RESGATE) + Σ(RENDIMENTO) — calculado em `prisma-investimento.repository.ts:28-43`
- ✅ Filtro `ativo=true` por padrão em `list-posicoes.use-case.ts:12`
- ✅ Filtro `pessoaId` funcional

### POST /api/investimentos/posicoes

- ✅ Valida categoria contra `CATEGORIAS_VALIDAS` — `create-posicao.use-case.ts:12-14`
- ✅ Rejeita `instituicao` vazia com 400 — `create-posicao.use-case.ts:17-19`
- ✅ Retorna 201 com métricas zeradas

### POST /api/investimentos/movimentacoes

- ✅ Rejeita `valor ≤ 0` com 400 — `create-movimentacao.use-case.ts:12`
- ✅ Rejeita `mesRef` inválido com 400 — `create-movimentacao.use-case.ts:13`
- ✅ Rejeita `tipo` inválido com 400 — `create-movimentacao.use-case.ts:14-16`
- ✅ Retorna 404 se posição não encontrada
- ✅ Rejeita movimentação em posição inativa com 400

### GET /api/investimentos/evolucao

- ✅ Array com exatamente N entradas (uma por mês)
- ✅ Saldo cumulativo inclui meses anteriores à janela
- ✅ Rejeita `meses` fora do range [1, 60] com 400
- ✅ Retorna array com zeros quando sem dados

### DELETE /api/investimentos/movimentacoes/:id

- ✅ 204 OK — `delete-movimentacao.use-case.ts:8`
- ✅ 404 se não existir — `delete-movimentacao.use-case.ts:7`

### DELETE /api/investimentos/posicoes/:id (desativar)

- ✅ Soft delete: `ativo=false` — `prisma-investimento.repository.ts:76-80`
- ✅ Posição inativa não aparece em GET padrão (ativo=true por default)
- ✅ Movimentações históricas preservadas (CASCADE não dispara em deactivate)

### Contratos arquiteturais

- ✅ `domain/` não importa Fastify, Prisma ou SDK: verificado em todas as 10 entidades/repos/entities
- ✅ `mesRef` sempre `YYYY-MM` em todo o código
- ✅ Valores Float em reais (não centavos)
- ✅ `toDomain()` inline em ambos os repos
- ✅ `HttpError` direto nos use cases (sem Result pattern)

---

## Frontend — Validações

### evolData não é array vazio

- ✅ `EvolucaoChart` recebe `data: EvolucaoPatrimonio[]` da API — `InvestimentosClient.tsx:74-79`
- ✅ `/api/investimentos/evolucao?meses=12` chamado no `useEffect` — não usa array fixo

### Modal de movimentação

- ✅ Botão `Registrar` disabled quando `valor ≤ 0` — `MovimentacaoForm.tsx:62` (`valorNum > 0`)
- ✅ Botão disabled quando posição não selecionada — `MovimentacaoForm.tsx:60` (`form.investimentoId !== ''`)

### Chips de tipo

- ✅ APORTE → bg `#10F5A3`, cor `#0A1A12` (texto escuro) — `InvestimentosClient.tsx:43`
- ✅ RENDIMENTO → bg `#7B6EF5`, cor `#fff` — `InvestimentosClient.tsx:44`
- ✅ RESGATE → bg `#F23A0A`, cor `#fff` — `InvestimentosClient.tsx:45`

### mesRef em formato YYYY-MM

- ✅ `MovimentacaoForm` usa `type="month"` que retorna YYYY-MM
- ✅ Todos os `apiFetch` passam `mesRef` como string YYYY-MM

### Valores formatados como R$ 1.234,56

- ✅ `formatMoney()` de `@/components/ui/MoneyValue` usado em toda a UI

### page.tsx é Server Component

- ✅ Sem `'use client'` em `page.tsx` — confirmado
- ✅ `InvestimentosClient.tsx` tem `'use client'` no topo

### apiFetch usado (sem fetch direto)

- ✅ Todos os calls usam `apiFetch` de `@/shared/lib/api`

---

## Achados não-bloqueantes

| Arquivo | Severidade | Observação |
|---------|-----------|------------|
| `InvestimentosClient.tsx:85` | 💡 BAIXO | `useEffect` com `selectedTab` não re-busca pessoas ao trocar tab — não é bug pois pessoas não mudam com tab |
| `prisma-movimentacao-investimento.repository.ts:85` | 💡 BAIXO | Saldo cumulativo calculado em memória — para volumes grandes, considerar query SQL agregada |

---

## Resumo

| Módulo | Status |
|--------|--------|
| Backend — posicoes (CRUD) | APROVADO |
| Backend — movimentacoes (CRUD) | APROVADO |
| Backend — evolucao | APROVADO |
| Backend — contratos arquiteturais | APROVADO |
| Frontend — charts com dados reais | APROVADO |
| Frontend — modais + validações | APROVADO |
| Frontend — chips de tipo | APROVADO |
| Frontend — Server Component | APROVADO |
| Build (tsc --noEmit API) | APROVADO |
| Build (npm run build web) | APROVADO |

**Veredicto geral: PASSOU**
