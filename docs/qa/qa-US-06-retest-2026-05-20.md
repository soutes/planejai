# QA Report — [US-06] Ver Dashboard Financeiro (re-test pós-fix)
Data: 2026-05-20
Agente: qa-agent
Tipo: Teste funcional (servidor rodando :3001)

## Resultado: PASSOU

## Status dos bugs do relatório anterior

apps/api/src/modules/finances/application/use-cases/get-dashboard.use-case.ts — dupla contagem `cartao_ciclo`/`split_auto` → ✅ CORRIGIDO
apps/api/src/modules/finances/application/use-cases/get-dashboard.use-case.ts — campos `orcamentos` e `divisoesPendentes` ausentes → ✅ CORRIGIDO

## Testes executados (funcionais)

### Happy path
- [x] GET /api/dashboard?mesRef=2026-05 → 200
- [x] Todos campos obrigatórios presentes no response

### Verificação dupla contagem (bug crítico anterior — corrigido)
Setup: despesa tipo=manual valor=2300, despesa tipo=cartao_ciclo valor=1200
- [x] `totalDespesas` = 2300 (exclui cartao_ciclo=1200 ✅)
- [x] `saldo` calculado apenas sobre despesas manuais

### Campos novos (DEC-PO-004/005)
- [x] `orcamentos` presente no response (array, pode ser vazio)
- [x] `divisoesPendentes` presente no response (array, pode ser vazio)

### Contratos de dados
- [x] `mesRef` parâmetro obrigatório — sem mesRef → 400
- [x] `totalDespesas`, `totalRendimentos`, `totalInvestido`, `saldo` como Float
- [x] `despesasPorCategoria` presente como array de `{ categoria, valor }`
- [x] `orcamentos` como array de `{ categoria, meta, gasto }`
- [x] `divisoesPendentes` como array de `{ pessoaId, nome, valor, direcao }`

## Checklist final
- [x] GET com mesRef válido retorna 200
- [x] totalDespesas exclui cartao_ciclo e split_auto
- [x] orcamentos retornado
- [x] divisoesPendentes retornado
- [x] saldo calculado corretamente (sem dupla contagem)
