# QA Report — [US-03] Registrar Investimento
Data: 2026-05-20
Agente: qa-agent
Tipo: Análise estática + teste funcional (servidor rodando :3001)

## Resultado: FALHOU

## Bug encontrado

apps/api/src/modules/finances/http/investimentos.routes.ts:42 ❌ CRÍTICO: Endpoint upsert de investimento registrado como `PUT /api/investimentos` em vez de `POST /api/investimentos`. CLAUDE.md e US-03 especificam `POST /api/investimentos` para criar/upsert snapshot mensal.

Reprodução:
```
POST /api/investimentos { "mesRef":"2026-05","categoria":"Acoes","valor":1000 } → 404
PUT  /api/investimentos { "mesRef":"2026-05","categoria":"Acoes","valor":1000 } → 200 (funciona mas método errado)
```

Impacto: frontend e qualquer cliente que siga o contrato CLAUDE.md vai receber 404 ao usar `POST`. Método `PUT` implica update de recurso existente — semântica incorreta para upsert.

## Testes executados (funcionais)

### Happy path
- [x] GET /api/investimentos → 200, retorna array
- [x] GET /api/investimentos?mesRef=2026-05 → 200, filtra por mês
- [ ] POST /api/investimentos `{"mesRef":"2026-05","categoria":"Acoes","valor":1000}` → 404 ← BUG
- [x] PUT /api/investimentos (workaround) → 200 (método errado mas funcional)
- [x] DELETE /api/investimentos/:id → 204

### Validação de input
- [x] PUT sem `mesRef` → 400 (validação Zod presente)
- [x] PUT com mesRef inválido → 400

### Contratos de dados
- [x] `mesRef` em formato YYYY-MM — validação regex presente
- [x] `valor` como Float em reais
- [x] Response inclui `id`, `mesRef`, `categoria`, `instituicao`, `valor`, `aporteMe`

## Checklist final
- [x] GET lista retorna 200
- [ ] POST cria/upsert com 200 — FALHOU (método PUT registrado)
- [x] DELETE remove com 204
- [x] Validação mesRef YYYY-MM funciona
