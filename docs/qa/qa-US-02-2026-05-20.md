# QA Report — [US-02] Registrar Rendimento
Data: 2026-05-20
Agente: qa-agent
Tipo: Análise estática (pré-aprovação architect)

## Resultado: FALHOU

## Bugs encontrados

apps/api/src/modules/finances/http/rendimentos.routes.ts:69 ❌ CRÍTICO: Parâmetro de delete série usa `deleteAll` em vez de `serie`. Contrato US-02 especifica `DELETE /api/rendimentos/:id?serie=true`, mas a rota aceita `?deleteAll=true`. Reprodução: `DELETE /api/rendimentos/1?serie=true` não exclui a série — retorna 204 deletando apenas a instância.

apps/api/src/modules/finances/application/use-cases/create-rendimento.use-case.ts:8 ⚠️ MÉDIO: Rendimento com `recorrente=true` não propaga para os próximos N meses. US-02 diz: "ao criar com `recorrente=true`, o sistema propaga para os próximos N meses automaticamente". O use case apenas cria o registro do mês atual. Reprodução: POST `/api/rendimentos` com `{ recorrente: true, totalRepeticoes: 3, mesRef: "2026-05" }` — apenas cria 1 registro em vez de 3.

## Happy path testado (estático)
- [x] GET lista — rota existe, filtro por mesRef presente
- [x] POST cria recurso com 201 — rota existe, validação presente
- [x] PUT atualiza recurso — rota existe
- [x] DELETE remove recurso com 204 — rota existe
- [ ] DELETE ?serie=true exclui série — parâmetro errado (`deleteAll` vs `serie`)
- [x] Valores mesRef em formato YYYY-MM — validação regex presente no body
- [x] Valores monetários como Float — sem conversão de centavos
- [ ] Recorrente propaga N meses — não implementado no use case
